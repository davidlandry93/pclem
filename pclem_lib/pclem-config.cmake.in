include(CMakeFindDependencyMacro)
find_dependency(glog)

get_filename_component(PCLEM_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

set(PCLEM_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

if(NOT TARGET pclem AND NOT pclem_BINARY_DIR)
  include("${PCLEM_CMAKE_DIR}/pclem-targets.cmake")
endif()

set(pclem_LIBRARIES pclem)
set(pclem_EXECUTABLE)
